{
    "builds": [{
        "src": "friendly_host/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "friendly_host/wsgi.py"
        }
    ]
}